plugins {
    id 'java'
}

group 'org.springframework'
version '5.1.13.BUILD-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    //表示引用 模块 spring-context
    compile(project(":spring-context"))
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
